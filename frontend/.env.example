# Backend API URL
# - Local dev: leave as-is (Vite proxy forwards /api → localhost:5000)
# - Production: set to your Render backend URL + /api/v1
VITE_API_URL=/api/v1

# Optional: full production example
# VITE_API_URL=https://shortly-p1ip.onrender.com/api/v1